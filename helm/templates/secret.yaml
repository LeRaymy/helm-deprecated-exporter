apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-kubeconfig
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  config: {{ required "A valid .Values.service.kubeconfig entry is required!" Values.service.kubeconfig }}
